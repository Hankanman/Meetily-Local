name: "Build Test"

on: workflow_dispatch

jobs:
  build-test:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform:
          - "ubuntu-22.04"
          - "ubuntu-24.04"

    uses: ./.github/workflows/build.yml
    with:
      platform: ${{ matrix.platform }}
      upload-artifacts: true
    secrets: inherit
